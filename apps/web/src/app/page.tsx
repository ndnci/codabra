import React from "react";

export default function Home(): React.JSX.Element {
    return (
        <main>
            <h1>Welcome to your Codabra app</h1>
            <p>
                Run <code>codabra generate</code> to scaffold pages and API routes from your <code>/config</code> files.
            </p>
        </main>
    );
}
