# web

This is the example Next.js application that Codabra generates code into.

**Do not edit files inside `src/types/`, `src/app/api/`, or `prisma/schema.prisma` manually** — they are auto-generated by `codabra generate`.

Edit the files in `/config` at the project root, then run:

```bash
codabra generate
```
